apiVersion: v2
name: myapp
description: A Helm chart for deploying api, rpc, and mongo services
version: 0.1.0
appVersion: "1.0"
dependencies:
  - name: mongodb
    version: "10.0.0"  # 选择适合的 MongoDB chart 版本
    repository: "https://charts.bitnami.com/bitnami"
